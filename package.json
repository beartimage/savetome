{
  "name": "savetome",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "saveto.me — smart bookmark manager SPA",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && wrangler deploy"
  },
  "devDependencies": {
    "vite": "^8.2.1",
    "wrangler": "^4.120.0"
  }
}
